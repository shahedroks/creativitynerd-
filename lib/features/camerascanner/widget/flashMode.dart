import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';
import 'package:pdf_scanner/core/constants/color_control/all_color.dart'; // Assuming AllColor is your custom color class

class Flashmode extends StatelessWidget {
  const Flashmode({Key? key}) : super(key: key);

  // Show the custom popup menu


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("data" ),

    );
  }
}
